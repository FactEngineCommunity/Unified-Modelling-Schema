unit UMSConstants;

interface

type
  TUMSDataType = (dtInteger, dtDateTime, dtTextFixedLength, dtTextVariableLength);
  TUMSConstraint = (ctNotNull, ctUnique);
  TUMSCardinality = (cdOne, cdMany, cdZeroOrOne, cdZeroOrMore);

implementation

end.
